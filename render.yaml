services:
  - type: web
    name: medmind
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd web_app && python manage.py migrate && gunicorn disease_predictor.wsgi:application --bind 0.0.0.0:$PORT
